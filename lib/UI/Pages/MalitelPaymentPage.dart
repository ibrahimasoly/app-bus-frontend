import 'package:flutter/material.dart';

class MalitelPaymentPage extends StatelessWidget {
  const MalitelPaymentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Malitel"),
      ),
    );
  }
}
